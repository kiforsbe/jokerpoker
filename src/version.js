// Game version shown in the page corner. Kept in sync with package.json
// by tests/version.test.js (src/ deploys standalone, so the page can't
// read package.json at runtime).
export const GAME_VERSION = '0.3.4';
