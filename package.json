{
  "name": "joker-poker",
  "version": "0.3.5",
  "description": "Browser remake of the classic Finnish Jokeripokeri arcade video poker machine, built with Three.js and the Web Audio API",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "npx http-server -p 5500 -c-1",
    "test": "node --test",
    "build:local": "node scripts/build.mjs local",
    "build:vendored": "node scripts/build.mjs vendored"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "esbuild": "^0.28.1",
    "three": "0.185.1"
  }
}
